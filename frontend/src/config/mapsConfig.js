/**
 * Configuración del sistema de mapas — Tuky v2.0
 *
 * Cambia USE_OSS_MAPS para alternar entre versiones:
 *   false → v1: Google Maps (Maps SDK + Places + Directions)  [ORIGINAL]
 *   true  → v2: OpenStreetMap + Photon + OSRM               [EXPERIMENTAL]
 */
export const USE_OSS_MAPS = true;
